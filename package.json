{
  "name": "server",
  "version": "1.0.0",
  "description": "Product App MERN project",
  "license": "ISC",
  "author": "Sameer",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --watch --env-file=.env server/index.js",
    "build": "npm install && npm install --prefix client && npm run build --prefix client",
    "run": "node --watch --env-file=.env index.js"
  },
  "dependencies": {
    "connect-mongo": "^6.0.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mongoose": "^9.3.0",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "router": "^2.2.0"
  }
}
